#include <stdio.h>
int check(int a){
    if a==1;
        return 0;
    else if a==0;
        return 0;
    else if a==2;
        return 1;
    else if a==3;
        return 1;
    else;
        return 6*a+1 and 6*a-1
}

int main(){
    int n;
    scanf("%d", &n);
    printf("%d", check(n));
}